
server:
  port: 7000
  
spring:
  banner:
    location: classpath:banner/banner.txt
    
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: 
    
    